# ApiDumper Configuration File
# Format: ruleName=ruleType:description

# Default rule that generates JSON using example values from schema report
generate-from-example=generate-from-example:Generate JSON using example values from schema report

# Missing properties rule - generates one JSON for each property with that property missing
missing-properties=missing-properties:Generate JSON examples with each property missing individually

# Null values rule - generates one JSON for each property with that property set to null
nullValues=nullValues:Generate JSON examples with each property set to null

# Empty values rule - generates one JSON for each property with that property set to empty
emptyValues=emptyValues:Generate JSON examples with each property set to empty (empty arrays, empty strings, empty objects)

# Future rules can be added here:
# random-data=random:Generate JSON with random values based on data types
# min-max-data=minmax:Generate JSON using min/max values from schema
# sample-data=sample:Generate JSON using sampled values from distinct values array
